import actions from './actions';

const pushSchedule = actions.pushSchedule;

const deleteSchedule = actions.deleteSchedule;

const updateSchedule = actions.updateSchedule;

export default {
    pushSchedule,
    deleteSchedule,
    updateSchedule,
}